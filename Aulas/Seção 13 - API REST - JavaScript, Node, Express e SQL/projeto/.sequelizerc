import { resolve } from 'path';

// Mapeamento de pastas do sequelize
export default {
    config: resolve(__dirname, 'src', 'config', 'database.js'), // dir database.js
    'models-path': resolve(__dirname, 'src', 'models'), // dir models
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // dir migrations
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // dir seeds
};